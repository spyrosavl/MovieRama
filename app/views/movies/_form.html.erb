<%= form_with(model: movie, local: true) do |form| %>
  <% if movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.text_field :title, id: :movie_title, required: true, autofocus: true, placeholder: "Movie title", class: "form-control form-control-lg" %>
  </div>

  <div class="form-group">
    <%= form.text_area :description, id: :movie_description, required: true, placeholder: "Movie description", class: "form-control form-control-lg" %>
  </div>

  <div class="actions">
    <button type="submit" class="btn btn-primary btn-lg float-right">Submit</button>
  </div>
<% end %>